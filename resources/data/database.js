import { DATABASE_DATABASE, DATABASE_HOST, DATABASE_PASSWORD, DATABASE_PORT, DATABASE_USER } from '../../utils/constants'

const database = {
  host: DATABASE_HOST,
  database: DATABASE_DATABASE,
  password: DATABASE_PASSWORD,
  port: DATABASE_PORT,
  user: DATABASE_USER
}

module.exports = database
